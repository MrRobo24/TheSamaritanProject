<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.identity_crisis.thesamaritans">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.WelcomeActivity"></activity>
        <activity android:name=".activity.profile.ProfileSettings" />
        <activity android:name=".activity.volunteerseeker.InterestedFormsActivity" />
        <activity android:name=".activity.profile.AccountInfo" />
        <activity android:name=".activity.profile.AddPost" /> <!-- <activity android:name=".activity.ViewRequestForm"></activity> -->
        <activity android:name=".activity.newsfeed.FeedInfo" />
        <activity android:name=".activity.volunteerseeker.PersonalRequestActivity" />
        <activity android:name=".activity.launcher.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.launcher.RegisterActivity" />
        <activity android:name=".activity.launcher.LoginActivity" />
        <activity android:name=".activity.volunteerseeker.SeekHelpFormActivity" />
        <activity
            android:name=".activity.connect.ChatLogActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activity.connect.NewMessageActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>